#ifndef MAIN_H
#define MAIN_H
#include "heap.h"
#include "graph.h"
#include "util.h"

//int main();

//Checks to make sure the given file exists
bool fexists(const char *filename);

#endif

